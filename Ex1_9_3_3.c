/* The aim is to sort an array without using any other array. It only contains 0 and 1*/

#include <stdio.h>
#include <stdlib.h>

void main(int argc, char* argv[]) {

    int tab[argc];

    for (int i = 0; i < argc; i++) {
        tab[i] = atoi(argv[i]);
    }

    for (int i = 0; i < argc; i++) {
        
    }

}